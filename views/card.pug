extends layout.pug

block content
    section#content
        //- ul
        //-     each color in colors
        //-         li= color
        h3= prompt
        if hint
            p 
                i Hint: #{hint}
        //- else
        //-     p (There is no hint on this one honey.)
    